import 'package:flutter/material.dart';

class AppColors {
  static const black = Color(0xFF000000);
  static const chedddarYellow = Color(0xFFF4A800);
  static const chedddarDark = Color(0xFFB87800);
  static const chedddarLight = Color(0xFFFFD050);
  static const holeColor = Color(0xFF7A4500);
}

class AppAssets {
  static const snapSound = 'assets/sounds/snap.mp3';
  static const cheeseModel = 'assets/models/cheese.obj';
}

// AdMob Unit IDs — replace with real IDs before release
class AdConfig {
  // Test IDs — swap for production IDs in release build
  static const bannerAdUnitId = 'ca-app-pub-3940256099942544/6300978111';
}
